import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;

/**
 * Created by icons on 5.3.17.
 */
public class MarksLength{
        public static void main(String[] args) {
            ArrayList<String> marksLength = new ArrayList<String>();
            marksLength.add("this");
            marksLength.add("is");
            marksLength.add("lots");
            marksLength.add("of");
            marksLength.add("fun");
            marksLength.add("for");
            marksLength.add("every");
            marksLength.add("Java");
            marksLength.add("programmer");

            Iterator<String> iter = marksLength.iterator();
            while (iter.hasNext()) {
                System.out.println(iter.next());
            }



            for (int i = 0; i < marksLength.size(); i++) {
                if (int length = marksLength.get.size()){
                    void add ( "****");
                }
                System.out.println(length);
            }

        }

}
    /*String str = "Это строка";
    int length = str.length();
            System.out.println(length);*/


